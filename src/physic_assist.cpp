#include "physic_assist.h"

void init_container(phys_cont* cont, sDisplay_info* obj_info) {
    /*
    cont->x = obj_info->x;
    cont->y = obj_info->y;
    if ( !(obj_info->surf == NULL) ) {
        cont->param.w_h->first = obj_info->surf->w;
        cont->param.w_h->second = obj_info->surf->h;
    }
    cont->obj_info = obj_info;

    //---DEFAULT SETTINGS---//
    cont->actorType = -1; //No other actor should match this
    cont->collType = 0; //Static
    cont->contType = 0; //Rectangle
    */
    return;
}
